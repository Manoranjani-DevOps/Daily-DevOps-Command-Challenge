# 🚀 Daily DevOps Command Challenge - Q7

### 🖥️ Scenario

A website has suddenly stopped working.

You SSH into the Linux server and check the Nginx service:

```bash
systemctl status nginx
```

Output:

```text
Active: failed (Result: exit-code)
```

❓Which is the **BEST** next command?

## A. `systemctl enable nginx`

## B. `reboot`

## C. `rm -rf /var/log`

## D. `journalctl -u nginx`

