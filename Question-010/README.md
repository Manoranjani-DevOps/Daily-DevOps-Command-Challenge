# 🚀 Daily DevOps Command Challenge - Q10

### 🖥️ Scenario

A developer accidentally staged the wrong file using:

```bash
git add config.yml
```

They want to remove the file from the **staging area** without deleting it from their local machine.

❓Which is the **BEST** command?

## A. `git reset HEAD config.yml`

## B. `git rm config.yml`

## C. `rm config.yml`

## D. `git clean -f`

