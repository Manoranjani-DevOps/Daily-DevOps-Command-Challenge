# 🚀 Daily DevOps Command Challenge - Q2

### 🖥️ Scenario

A user reports:

> "The application is running, but port **8080** is not reachable from another server."

You SSH into the Linux server.

❓Which is the **BEST** first command to verify whether the application is actually listening on **port 8080**?

## A. netstat -tulnp

## B. ss -tulnp | grep :8080

## C. df -h

## D. systemctl reboot
