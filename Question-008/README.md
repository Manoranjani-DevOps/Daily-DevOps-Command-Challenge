# 🚀 Daily DevOps Command Challenge - Q8

### 🖥️ Scenario

A website hosted on an AWS EC2 instance is not responding.

You successfully SSH into the server and run:

```bash
curl http://localhost
```

Output:

```text
curl: (7) Failed to connect to localhost port 80: Connection refused
```

❓Which is the **BEST** next command?

## A. `systemctl restart nginx`

## B. `aws ec2 reboot-instances`

## C. `systemctl status nginx`

## D. `nginx -t`

