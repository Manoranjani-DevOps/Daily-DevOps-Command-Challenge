# 🚀 Daily DevOps Command Challenge - Q14

### 🖥️ Scenario

A user reports:

> "The application service starts successfully, but I want to make sure it automatically starts after every server reboot."

❓Which is the **BEST** command?

## A. `systemctl stop nginx`

## B. `systemctl restart nginx`

## C. `systemctl enable nginx`

## D. `systemctl disable nginx`

